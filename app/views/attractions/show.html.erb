<%= content_for :actions do %>
  <li><%= link_to 'Add Attraction', new_trip_area_attraction_path(@trip.id, @area.id) %></li>
  <li><%= link_to 'Edit', edit_trip_area_attraction_path(@trip.id, @area.id, @attraction.id) %></li>
  <li><%= link_to 'Back', trip_area_path(@trip.id, @area.id) %></li>
<% end %>

<div class="row">
  <div class="col-md-12">
    <%= render 'attractions/attraction', attraction: @attraction %>
  </div>
</div>

</div>
