<%= content_for :actions do %>
  <li><%= link_to 'Show', trip_areas_path(@trip, @area) %></li>
  <li><%= link_to 'Back', trip_path(@trip) %></li>
<% end %>

<h1>Editing area</h1>

<%= render 'form' %>
