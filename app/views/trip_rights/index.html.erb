<h1>Listing trip_rights</h1>

<table>
  <thead>
    <tr>
      <th>Trip</th>
      <th>User</th>
      <th>Permission</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trip_rights.each do |trip_right| %>
      <tr>
        <td><%= trip_right.trip %></td>
        <td><%= trip_right.user %></td>
        <td><%= trip_right.permission %></td>
        <td><%= link_to 'Show', trip_right %></td>
        <td><%= link_to 'Edit', edit_trip_right_path(trip_right) %></td>
        <td><%= link_to 'Destroy', trip_right, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trip right', new_trip_right_path %>
