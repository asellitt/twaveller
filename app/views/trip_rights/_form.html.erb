<%= form_for([@trip, @trip_right]) do |f| %>
  <% if @trip_right.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip_right.errors.count, "error") %> prohibited this trip_right from being saved:</h2>

      <ul>
      <% @form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.select :user_id, options_for_select(@form.users, @form.user_id) %>
  </div>
  <div class="field">
    <%= f.label :permission %><br>
    <%= f.select :permission, options_for_select(@form.permissions, @form.permission) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
