<%= content_for :actions do %>
  <li><%= link_to 'Show', [@trip.id, @trip_right.id] %></li>
  <li><%= link_to 'Back', trip_trip_rights_path %></li>
  <li><%= link_to 'Delete', [@trip.id, @trip_right.id], method: :delete, data: { confirm: 'Are you sure?' } %></li>
<% end %>
<h1>Editing permission</h1>

<%= render 'form' %>

